# Maintenance Roadmap

> Last updated: 2026-08-07

This roadmap records engineering maintenance state and next priorities for zilan-agent. It is not platform validation evidence. Platform status remains governed by `agents/openai.yaml` and `docs/platform-validation.md`.

## Current Phase

`zilan_contract` productization is complete for the v2.5.7 package surface. The package now has guarded public API, module CLI, console script, schema reference, reusable examples, installed-package smoke tests, and wheel-install smoke tests. Future `zilan_contract` work should be compatibility maintenance or a separately designed schema/API version, not continued open-ended productization.

Remaining boundaries:

- installed `zilan_contract` checks deterministic output contracts; it is not an LLM judge, semantic grader, Buddhist doctrinal authority, or provider runtime validator;
- the v2.5.7 schema is exact-match only and does not support regex, fuzzy matching, Unicode normalization, tokenization, semantic similarity, or nested boolean logic;
- package smoke tests do not promote OpenAI, OpenAI-compatible, Claude Code, Codex, DeepSeek, GLM, or Qwen platform status;
- local Agama Markdown remains a searchable working corpus; publication-level claims still require CBETA XML-P5 and parallel-text collation.

Next mainline: return to reasoning and retrieval quality. The highest-ROI work is to keep `SRQ-*` answer contracts aligned with structured `ZR-*` validators, add transcript-backed runtime evidence only when prompt/runtime behavior changes, and advance citation-preserving Agama retrieval/collation before adding API services, UI layers, vector infrastructure, or broader provider claims.

## Current Baseline

| Area | Current state |
| --- | --- |
| CI | GitHub Actions runs repository invariants, generated Agama idempotency checks, pytest, ruff, mypy, and Agama search smoke tests. |
| Repository invariants | `scripts/validate_zilan_repo.py` checks required files, context files, YAML shape, regression inventory, platform status consistency, agent prompt contracts, runtime-evidence index references, runtime answer-review batch `answer_file` safety, and Agama search behavior; shared YAML/list-shape helpers live in `scripts/zilanlib/yaml_io.py`; repository path/version helpers live in `scripts/zilanlib/repository.py`; document-fragment checks use `scripts/zilanlib/text_checks.py`; agent prompt contract helpers live in `scripts/zilanlib/validation/agent_prompts.py`; platform metadata validation helpers live in `scripts/zilanlib/validation/platform.py`; regression-case schema validation helpers live in `scripts/zilanlib/validation/regression_cases.py`; reasoning-case schema validation helpers live in `scripts/zilanlib/validation/reasoning_cases.py`; retrieval-chunk fixture validation helpers live in `scripts/zilanlib/validation/retrieval_chunks.py`; Agama corpus validation helpers live in `scripts/zilanlib/validation/agama_corpus.py`; runtime-evidence validation helpers live in `scripts/zilanlib/validation/runtime_evidence.py`. |
| Regression inventory | `CODEX_REGRESSION_TESTS.md` is mirrored by `tests/regression_cases.yaml`; CI validates structure, resources, and case IDs. |
| Reasoning contract | `docs/architecture/reasoning-contract.md` defines the v0 structured reasoning contract; `docs/architecture/reasoning-contract-review.md` maps current ZC evidence to `ZR-*` cases without claiming a new runtime pass; `docs/architecture/post-contract-runtime-review.md` records committed-evidence gaps against `SRQ-02` through `SRQ-04`; `docs/architecture/zilanlib-extraction-cleanup-review.md` closes the earlier broad `zilanlib` extraction sweep; `docs/superpowers/plans/2026-07-22-zilan-maintenance-deepening.md` records the narrower follow-up plan for concrete validation-script, harness, coverage, and packaging maintenance; `tests/reasoning_cases.yaml` stores schema-validated seed fixtures. |
| Platform status | `agents/openai.yaml` is the machine-readable source; `docs/platform-validation.md` is the human-readable validation record. Codex is `tested`; Claude Code is `tested` as of the 2026-06-18 post-contract full rerun when Windows PowerShell stdin is forced to UTF-8. |
| Runtime validation | `docs/runtime-validation-log.md` records manual runtime validation sessions and transcript availability, including the 2026-06-18 Claude Code post-contract target review, Agama contract fix review for `SRQ-04` / `ZC-05`, Madhyamaka contract fix review for broad `ZC-05`, post-contract full `ZC-01` through `ZC-06` rerun, 2026-07-13 `SRQ-11` Collected Topics definition-scope spot review, 2026-07-14 `SRQ-04` / compact `ZC-04` Agama citation-boundary spot review, the 2026-07-14 compact `ZC-04` boundary rerun after prompt wording hardening, the 2026-07-14 post-prompt Claude Code `ZC-01` through `ZC-06` rerun, and the 2026-07-14 post-#124 broad boundary spot review showing broad `ZC-05` now passes `SRQ-03`, `SRQ-04`, and `SRQ-08`, and the 2026-07-14 post-#126 broad `ZC-04` rerun showing `SRQ-04` now passes. |
| Runtime evidence policy | `docs/validation-evidence.md` defines evidence levels, transcript redaction, and status-promotion rules. |
| Runtime evidence excerpts | `docs/runtime-evidence/` stores small redacted command-output or transcript excerpts that support validation-log entries. Contract-reviewable runtime answers should use standalone `*-answer.md` excerpts; summary-only evidence must not be used as `answer_file` input for batch or contract review. |
| Architecture overview | `ARCHITECTURE.md` provides the contributor-facing architecture entrypoint for the Skill/Agent dual track, `zilanlib` CLI-wrapper pattern, fixture-only validators, platform-status boundaries, and local validation workflow. |
| Coverage baseline | `pytest-cov` is available in dev dependencies and `python -m pytest` reports `scripts/zilanlib` coverage without a fail-under gate. The current 2026-08-04 local zilanlib-only baseline is 85%; root CLI wrappers remain exercised by tests but no longer dominate the reported coverage surface. |
| Contribution intake | `.github/ISSUE_TEMPLATE/` provides structured bug-report and feature-request templates for platform, validation, documentation, CI, and reasoning-contract work. |
| Community conduct | `CODE_OF_CONDUCT.md` adopts Contributor Covenant 2.1 with maintainer contact via GitHub `@RyanYao527`, and `CONTRIBUTING.md` / `CONTRIBUTING-en.md` link to it. |
| Community deliverables | `docs/article-output-contracts.md` is an LLM-engineering-facing article pitching the output-contract pattern; `docs/zilan-contract-quickstart.md` is a standalone quickstart for the `zilan_contract` pip package; `docs/awesome-list-pr-entries.md` provides pre-written PR entries for four curated awesome lists; `docs/codex-manual-tasks.md` is a checklist for recording demos, uploading HF datasets, submitting awesome-list PRs, and publishing the article. |
| `zilan_contract` product surface | The package now exposes public result helpers, `AnswerContractRunner` for domain-neutral required/forbidden term and slot checks, `zilan-contract` / `python -m zilan_contract.cli` check commands with JSON and Markdown issue details, contract schema validation, reusable medical/legal/financial examples, installed-package smoke coverage, console-script smoke coverage, and wheel-install smoke coverage without provider calls or platform-status changes. |
| `zilan_contract` productization closeout | `docs/architecture/zilan-contract-productization-closeout.md` records the P2 closeout baseline, completion criteria, package verification guardrails, and non-goals for provider validation, semantic grading, and schema-v2 expansion. |
| Co-maintainer path | `README.md`, `README.zh.md`, `README.en.md`, `CONTRIBUTING.md`, and `CONTRIBUTING-en.md` now invite regular collaborators and define reviewable co-maintainer work areas such as documentation, runtime validation, Agama collation, and reasoning-contract review. |
| Installation docs | `docs/installation.md` separates Codex, Claude Code, and OpenAI API operating paths. |
| Clean install smoke | A 2026-06-15 clean clone from GitHub passed repository checks, pytest, ruff, OpenAI dry-run, and Agama search smoke tests when run sequentially. Current CI also includes a minimal mypy type-check baseline for `scripts/`. |
| Mock Claude install | `scripts/mock_install_smoke.py` verifies the Claude Code skill/agent install layout in a temporary mock home without touching the real user profile. |
| OpenAI API harness | `scripts/openai_api_harness.py` builds dry-run or live Responses API requests from `agents/openai.yaml` and regression cases; live native OpenAI runs require `OPENAI_API_KEY`. |
| OpenAI-compatible harness | The same harness can target configurable OpenAI-compatible `chat-completions` endpoints such as Volcengine without upgrading native OpenAI API status; Volcengine ZC-01 through ZC-03 live validation is recorded as of 2026-06-16. |
| Provider route triage | `docs/provider-routes.md` keeps OpenAI API, DeepSeek, GLM, and Qwen route claims conservative until live evidence exists. |
| Agama search | `scripts/search_agama.py` remains the stable CLI and compatibility surface; reusable Markdown search APIs live in `scripts/zilanlib/agama/search.py`. The search path filters known false positives, supports passage grouping, emits JSON, preserves `section_marker` / `section_title` metadata and canonical `section_label` when available, and provides stable `citation` / `passage_citation` fields. |
| CBETA XML-P5 collation preflight | `scripts/cbeta_collation_preflight.py` remains a local, read-only preflight wrapper while `scripts/zilanlib/agama/collation_preflight.py` checks the publication-level route from generated Agama Markdown views back to committed CBETA XML-P5 sources, TEI headers, publication statements, and source descriptions. It intentionally does not perform publication-level collation, align Markdown line anchors to XML body anchors, compare parallel texts, call providers, introduce vector infrastructure, or change platform validation status. Design details live in `docs/architecture/cbeta-xml-p5-collation-preflight.md`. |
| Semantic retrieval fixtures | `tests/fixtures/retrieval_chunks/semantic_chunks.yaml` defines fixture-only chunks and query expectations; repository validation checks source files, line ranges, local citation anchors, Agama provenance hashes, reasoning roles, expected chunk IDs, non-chunk answer-boundary contracts, answer contracts, required answer slots, and checked-in answer sample references without introducing embeddings or vector storage. `SRQ-01` now includes reviewed `雜阿含經` / `長阿含經` Agama evidence chunks plus Collected Topics and Madhyamaka prasaṅga context chunks; `practice_boundary` remains a non-chunk answer boundary need. `SRQ-02` covers a narrow Hetuvidya error-detection gap by routing a `reason_unestablished` 论式 to the trairupya context and `ZR-03` fixture. `SRQ-05` covers a narrow Hetuvidya `不周遍` gap by routing a `non_pervasive` 论式 to the trairupya context and `ZR-07` fixture. `SRQ-06` covers a narrow Hetuvidya `不定因` gap by routing an `inconclusive_or_contradictory` 论式 to the trairupya context and `ZR-08` fixture. `SRQ-07` covers a narrow Collected Topics total/part error by routing a local-report-feedback overgeneralization to the total/part context and `ZR-02` fixture. `SRQ-11` covers a narrow Collected Topics definition-scope error by routing `瓶的性相是能盛水者` to the defining-mark / definiendum context and `ZR-12` fixture. `SRQ-09` covers a narrow cognitive-analysis and practice-boundary case by routing work-feedback distress to five-universal, cognitive-quality, and vipassana context chunks plus the `ZR-10` fixture. `SRQ-10` extends the same precise boundary-slot pattern to caregiving-pressure attribution by routing family-care stress to five-universal, caregiving-pressure, vipassana-caregiving, and `ZR-11` chunks. `SRQ-03` covers a narrow Madhyamaka prasaṅga-boundary gap by routing a `诸法自性有` premise to the prasaṅga method context and `ZR-04` fixture. `SRQ-08` covers a Madhyamaka nihilism-boundary gap by routing an emptiness-cancels-causality claim to the prasaṅga method context and `ZR-09` fixture. `SRQ-04` covers a narrow Agama citation-boundary gap by routing a four-Agama no-self query to representative Agama evidence chunks, including a reviewed `長阿含經` 卷 10 three-feelings non-self passage, and the `ZR-05` fixture, while requiring CBETA/local anchors, search-scope language, representative-status language, and a collation boundary. `tests/fixtures/answers/` provides tiny committed pass/fail answer samples for boundary review and SRQ-01 through SRQ-11 answer-contract review. `scripts/semantic_retrieval_dry_run.py` remains the stable CLI wrapper while `scripts/zilanlib/semantic/retrieval_dry_run.py` returns expected chunks for query fixtures as an executable contract proof. `scripts/semantic_fixture_candidates.py` remains the stable CLI wrapper while `scripts/zilanlib/agama/candidates.py` builds reviewable `agama_passage` candidates from `search_agama.py` baseline hits with `source_hash`, `line_text_hash`, and provenance metadata. `scripts/semantic_fixture_review.py` remains the stable CLI wrapper while `scripts/zilanlib/agama/fixture_review.py` reports represented candidates, range matches, provenance/hash drift, and fixture-only Agama chunks without overwriting YAML. `scripts/semantic_context_bundle.py` remains the stable CLI wrapper while `scripts/zilanlib/semantic/context_bundle.py` assembles selected chunks into prompt-ready order. `scripts/semantic_role_coverage.py` remains the stable CLI wrapper while `scripts/zilanlib/semantic/role_coverage.py` compares query `needs` with selected chunk roles. `scripts/semantic_answer_boundary_review.py` remains the stable CLI wrapper while `scripts/zilanlib/semantic/answer_boundary_review.py` checks downstream answer text or checked-in samples against non-chunk boundary contracts. `scripts/semantic_answer_contract_review.py` remains the stable CLI wrapper while `scripts/zilanlib/semantic/answer_contract_review.py` checks downstream answer text or checked-in samples against explicit answer terms and shallow answer slots. |
| Hetuvidya validator prototype | `scripts/hetuvidya_validator.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/hetuvidya_validator.py` reads only structured `tests/reasoning_cases.yaml` Hetuvidya fixtures and emits deterministic JSON/text results for subject, predicate, reason, legacy three-check status, structured trairupya checks, judgment status, and diagnostics. It intentionally does not parse arbitrary natural-language arguments, call providers, alter prompts, or grade doctrine. |
| Collected Topics analyzer prototype | `scripts/collected_topics_analyzer.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/collected_topics_analyzer.py` reads only structured `tests/reasoning_cases.yaml` Collected Topics fixtures and emits deterministic JSON/text analyses for `concepts`, `relation_checks`, `error_type`, and diagnostics. It intentionally does not parse arbitrary natural language, call providers, alter prompts, or grade doctrine. |
| Madhyamaka critique engine prototype | `scripts/madhyamaka_critique_engine.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/madhyamaka_critique_engine.py` reads only structured `tests/reasoning_cases.yaml` Madhyamaka prasaṅga fixtures and emits deterministic JSON/text critiques for `opponent_premise`, `accepted_commitments`, `contradictions`, `no_independent_thesis`, `critique_steps`, and diagnostics. It intentionally does not parse arbitrary natural language, call providers, alter prompts, prove ultimate claims, or grade doctrine. |
| Cognitive-analysis mapper prototype | `scripts/cognitive_analysis_mapper.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/cognitive_analysis_mapper.py` reads only structured `tests/reasoning_cases.yaml` cognitive-analysis fixtures and emits deterministic JSON/text mappings for the five-universal chain, afflictions, corrective factors, and practice-boundary requirement. It intentionally does not parse arbitrary natural language, call providers, provide clinical advice, alter prompts, or grade doctrine. |
| Agama evidence checker prototype | `scripts/agama_evidence_checker.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/agama_evidence_checker.py` reads structured `tests/reasoning_cases.yaml` Agama evidence fixtures and checked-in semantic retrieval chunks, then emits deterministic JSON/text checks for citation requirements, representative-search scope, local `context/agama/` references, collation boundaries, local Markdown file existence, CBETA ID consistency, line ranges, and fixture text anchors. It includes negative fixture coverage for bad line ranges, mismatched CBETA IDs, and missing text anchors. It intentionally does not run Agama search, perform CBETA XML collation, call providers, alter prompts, or grade citation correctness. |
| Reasoning validator output envelope | `scripts/reasoning_validator_output.py` remains a compatibility shim while `scripts/zilanlib/reasoning/validator_output.py` provides the shared validator output envelope for run and not-applicable statuses. It intentionally only shapes local fixture-validator metadata and does not grade answer quality, doctrine, or platform behavior. |
| Reasoning contract runner | `scripts/reasoning_contract_runner.py` remains the stable CLI wrapper while `scripts/zilanlib/reasoning/contract_runner.py` combines the semantic retrieval dry run, role coverage, answer-contract review, Hetuvidya validator, Collected Topics analyzer, Madhyamaka critique engine, cognitive-analysis mapper, and Agama evidence checker into one fixture-only local entrypoint. Its validators now expose a common top-level output envelope (`status`, `validator`, `contract_family`, `source`, `case_ids`, `limitations`) while preserving family-specific payload keys. Its `pass` / `fail` / `review_needed` status is a contract-review status, not platform validation or doctrinal grading. |
| Reasoning answer review | `scripts/reasoning_answer_review.py` remains the stable single-review CLI wrapper while `scripts/reasoning_answer_review_batch.py` runs the same compact review over a YAML batch manifest for transcript or answer-excerpt review sets. The library modules live in `scripts/zilanlib/reasoning/answer_review.py` and `scripts/zilanlib/reasoning/answer_review_batch.py`. They summarize role coverage, answer-contract misses, answer-validator alignment drift, selected validator families, and fixture-only limitations for manual review without generating answers, calling providers, altering prompts, grading doctrine, or changing platform validation status. |
| Agent prompts | Codex and Claude agent prompts are checked for the Agama citation contract, Collected Topics total/part boundary contract, cognitive-analysis / vipassana practice-boundary contract, Madhyamaka prasaṅga output contract, activation/task merge behavior, and `search_agama.py --json` citation-field preference. |
| Release notes | `CHANGELOG.md` records project-level release changes. |

## Operating Rules

- Run `python scripts/validate_zilan_repo.py --check-generated --strict-yaml`, `python -m pytest`, `python -m ruff check scripts tests`, and `python -m mypy` before merging prompt, context, script, or platform metadata changes.
- Run generated-file checks sequentially rather than in parallel with pytest; `--check-generated` may rebuild committed Agama Markdown during validation.
- Keep platform claims conservative. Do not mark a route `tested` without dated validation evidence in `docs/platform-validation.md`.
- Use local Agama Markdown as a searchable working corpus. Use `_source/` XML only for collation, source verification, or CBETA-specific checks.
- Preserve stable citation output in `search_agama.py`; downstream prompts and regression expectations depend on `citation` and `passage_citation`.
- Prefer small PRs with one maintenance theme each, and keep unrelated content or wording refactors out of engineering PRs.

## Near-Term Priorities

| Priority | Track | Work | Done when |
| --- | --- | --- | --- |
| P0 | Runtime validation | Re-run ZC-01 through ZC-06 after prompt or routing changes and append to `docs/runtime-validation-log.md`. | A dated manual validation note records prompts, observed behavior, failures, transcript status, and checks run. Latest Claude Code full rerun is recorded for 2026-07-14 after root-document and closing-wording changes, with broad-answer contract gaps tracked as follow-up. |
| P1 | Validation evidence | Replace summarized baselines with transcript-backed Codex and Claude Code sessions where practical. | Runtime results are auditable without relying on chat history. |
| P1 | Claude Code route | Keep the UTF-8 stdin validation protocol visible and rerun exact ZC prompts after prompt, tool, or install-path changes. | Claude Code remains `tested` only while dated evidence documents the exact prompts, encoding setup, known limits, and repository checks. |
| P1 | OpenAI API route | Run the minimal harness in live mode with `OPENAI_API_KEY` and record a dated response summary. | OpenAI API can move from `harness-ready` to `tested` only after live evidence is recorded. |
| P1 | Volcengine compatible route | Expand the 2026-06-16 ZC-01 through ZC-03 live pass to ZC-04 through ZC-06 only if broader provider-route confidence is needed. | Volcengine evidence remains separated from native OpenAI API validation, with provider/model details and limitations recorded. |
| P1 | Provider routes | Add native dry-run/live harnesses for DeepSeek, GLM, and Qwen, or record a concrete blocked state. | Each route has a dated tested or blocked entry with provider/model details and failure modes. |
| P1 | Reasoning quality | Preserve broad `ZC-05` coverage for `SRQ-03`, `SRQ-04`, and `SRQ-08` after #124 prompt hardening and the `SRQ-03` forbidden-term conflict fix. | Broad `ZC-05` continues to satisfy Agama citation, prasaṅga, and nihilism-boundary contracts without changing platform status. |
| P1 | Next mainline | Resume reasoning/retrieval quality after the `zilan_contract` productization closeout: maintain answer-contract / structured-validator alignment, capture transcript-backed runtime evidence when behavior changes, and improve citation-preserving Agama retrieval. | New work strengthens SRQ/ZR coverage or citation evidence without reopening package productization or changing platform status. |
| P1 | Semantic retrieval | Preserve the post-#126 broad `ZC-04` direct Agama-summary pass and keep future prompt changes from dropping literal `检索范围`, `T02n0099`, `CBETA`, `context/agama/`, `代表性`, and `待校勘` slots. | Direct `SRQ-04`, compact `ZC-04`, broad direct `ZC-04`, generated reports, and broad `ZC-05` satisfy the `SRQ-04` contract without changing platform status. |
| P1 | Agama citations | Extract or preserve finer-grained sutra or section markers when present in the Markdown. | Search output can cite representative passages beyond file, line, fascicle, and canonical section labels where the source supports it. |
| P2 | Scholarly collation | Extend the local CBETA XML-P5 preflight into narrower manual collation aids, starting with Markdown-line to XML-anchor checks and a small parallel-text candidate map for high-value no-self passages. | Publication-level work has dated evidence that specific cited passages were checked against XML-P5 and relevant parallels; preflight alone is not a collation pass. |
| P2 | Installation docs | Keep install paths and activation expectations current after platform changes. | New users can install the skill or agent without reading implementation history. |
| P2 | `zilan_contract` compatibility maintenance | Treat the v2.5.7 productization sweep as closed; future package work should preserve API, module CLI, console script, schema, examples, and installed/wheel smoke behavior. | Package-surface changes include targeted installed-package tests and do not change platform-validation status. |
| P2 | Release hygiene | Keep `CHANGELOG.md` updated for user-visible changes. | Changes can be summarized for users without reading merged PRs. |
| P2 | zilanlib cleanup | Continue only with narrow, behavior-preserving maintenance PRs documented in `docs/superpowers/plans/2026-07-22-zilan-maintenance-deepening.md`; the current concrete pain is validation-script size, harness YAML duplication, coverage scope, and packaging metadata. | Root scripts remain stable CLIs, reusable helpers live under `scripts/zilanlib/`, and each further extraction has tests plus a rollback path. |

## Manual Validation Checklist

Use this checklist before changing a platform route from metadata/config status to tested:

1. Record the exact date, provider, model, tool/runtime version, and repository commit.
2. Run the relevant ZC prompts from `CODEX_REGRESSION_TESTS.md` or a documented equivalent.
3. Confirm context loading, citation behavior, boundary statements, and sub-agent routing where applicable.
4. Record failures and follow-up fixes, not only successful final answers.
5. Run the repository checks listed in this document.
6. Update `agents/openai.yaml` and `docs/platform-validation.md` in the same PR.

## Backlog Guardrails

- Do not add broad abstractions unless they protect a real contract already used by prompts, scripts, tests, or documentation.
- Do not treat smoke tests as answer-quality grading. Manual runtime review remains required for agent behavior.
- Do not upgrade scholarly claims beyond the evidence in the local corpus and documented collation route.
